syntax = "proto3";
package vehiculos;
message DisponibilidadRequest { string vehiculo_id = 1; }
message DisponibilidadResponse { bool disponible = 1; string estado = 2; }
service VehiculosService {
  rpc VerificarDisponibilidad(DisponibilidadRequest) returns (DisponibilidadResponse);
}
